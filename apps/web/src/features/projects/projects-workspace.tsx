'use client';

import { useEffect, useMemo, useState } from 'react';
import type { Organization, OrganizationRole, Project } from '@tacit/core-schemas';

type OrganizationOption = Pick<Organization, 'id' | 'name' | 'slug' | 'mode'> & { readonly role: OrganizationRole };
interface Session { readonly accessToken: string; readonly email: string | null; }
const sessionKey = 'tacit.production.session';
function loadSession(): Session | null { try { const value = window.sessionStorage.getItem(sessionKey); return value ? JSON.parse(value) as Session : null; } catch { return null; } }
async function api<T>(path: string, token: string, init: RequestInit = {}): Promise<T> { const response = await fetch(path, { ...init, headers: { Authorization: `Bearer ${token}`, 'Content-Type': 'application/json', ...(init.headers ?? {}) } }); const body = await response.json() as T & { error?: string }; if (!response.ok) throw new Error(body.error ?? 'The request could not be completed.'); return body; }

export function ProjectsWorkspace() {
  const [session, setSession] = useState<Session | null>(null); const [organizations, setOrganizations] = useState<OrganizationOption[]>([]); const [projects, setProjects] = useState<Project[]>([]);
  const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [organizationName, setOrganizationName] = useState(''); const [projectName, setProjectName] = useState(''); const [organizationId, setOrganizationId] = useState(''); const [message, setMessage] = useState<string | null>(null); const [busy, setBusy] = useState(false);
  const url = process.env.NEXT_PUBLIC_SUPABASE_URL; const anonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY; const writableOrganizations = useMemo(() => organizations.filter(({ role }) => role !== 'viewer'), [organizations]);
  async function refresh(token = session?.accessToken) { if (!token) return; const [organizationResponse, projectResponse] = await Promise.all([api<{ organizations: OrganizationOption[] }>('/api/organizations', token), api<{ projects: Project[] }>('/api/projects', token)]); setOrganizations(organizationResponse.organizations); setProjects(projectResponse.projects); setOrganizationId((current) => current && organizationResponse.organizations.some(({ id }) => id === current) ? current : organizationResponse.organizations[0]?.id ?? ''); }
  useEffect(() => { const restored = loadSession(); setSession(restored); if (restored) void refresh(restored.accessToken).catch((error: Error) => setMessage(error.message)); }, []);
  async function signIn(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); if (!url || !anonKey) return setMessage('Authentication is not configured in this environment.'); setBusy(true); setMessage(null); try { const response = await fetch(`${url.replace(/\/$/, '')}/auth/v1/token?grant_type=password`, { method: 'POST', headers: { apikey: anonKey, 'Content-Type': 'application/json' }, body: JSON.stringify({ email, password }) }); const body = await response.json() as { access_token?: string; user?: { email?: string }; error_description?: string }; if (!response.ok || !body.access_token) throw new Error(body.error_description ?? 'Unable to sign in.'); const next = { accessToken: body.access_token, email: body.user?.email ?? email }; window.sessionStorage.setItem(sessionKey, JSON.stringify(next)); setSession(next); await refresh(next.accessToken); } catch (error) { setMessage(error instanceof Error ? error.message : 'Unable to sign in.'); } finally { setBusy(false); } }
  async function createOrganization(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); if (!session) return; setBusy(true); setMessage(null); try { await api('/api/organizations', session.accessToken, { method: 'POST', body: JSON.stringify({ name: organizationName }) }); setOrganizationName(''); await refresh(); } catch (error) { setMessage(error instanceof Error ? error.message : 'Unable to create organization.'); } finally { setBusy(false); } }
  async function createProject(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); if (!session || !organizationId) return; setBusy(true); setMessage(null); try { const response = await api<{ project: Project }>('/api/projects', session.accessToken, { method: 'POST', headers: { 'Idempotency-Key': crypto.randomUUID() }, body: JSON.stringify({ organizationId, name: projectName, workflowType: 'invoice_exception' }) }); window.location.assign(`/projects/${response.project.id}/evidence`); } catch (error) { setMessage(error instanceof Error ? error.message : 'Unable to create project.'); } finally { setBusy(false); } }
  if (!session) return <main className="auth-page"><section className="auth-card"><p className="eyebrow">Production workspace</p><h1>Sign in to Tacit</h1><p className="muted">Production projects are isolated by organization. The guided invoice experience remains separate in <a href="/demo">Demo mode</a>.</p><form className="stack" onSubmit={signIn}><label><span className="field-label">Email</span><input className="input" required type="email" value={email} onChange={(event) => setEmail(event.target.value)} /></label><label><span className="field-label">Password</span><input className="input" required type="password" value={password} onChange={(event) => setPassword(event.target.value)} /></label><button className="btn btn-primary" disabled={busy} type="submit">{busy ? 'Signing in…' : 'Sign in'}</button></form>{message ? <p className="notice" role="alert">{message}</p> : null}</section></main>;
  return <main className="production-page"><header className="production-header"><a className="brand" href="/"><span className="brand-mark">T</span><span>Tacit</span></a><div><span className="status status-success">Production mode</span><button className="btn btn-ghost" type="button" onClick={() => { window.sessionStorage.removeItem(sessionKey); setSession(null); setOrganizations([]); setProjects([]); }}>Sign out</button></div></header><section className="production-intro"><div><p className="eyebrow">Authenticated workspace</p><h1>Projects</h1><p className="muted">{session.email ?? 'Signed in'} · Run the supervised Invoice Exception Review pilot from evidence intake through human-approved recommendations.</p></div><a className="btn btn-secondary" href="/demo">Open read-only demo</a></section>{message ? <p className="notice" role="alert">{message}</p> : null}<section className="production-grid"><article className="card"><h2>Organization</h2>{organizations.length ? <label><span className="field-label">Active organization</span><select className="select" value={organizationId} onChange={(event) => setOrganizationId(event.target.value)}>{organizations.map((organization) => <option key={organization.id} value={organization.id}>{organization.name} ({organization.role})</option>)}</select></label> : <p className="muted">Create an organization to start a production project.</p>}<form className="inline-form" onSubmit={createOrganization}><input className="input" placeholder="Organization name" required value={organizationName} onChange={(event) => setOrganizationName(event.target.value)} /><button className="btn btn-secondary" disabled={busy} type="submit">Create organization</button></form></article><article className="card"><h2>New project</h2><p className="muted">Invoice Exception Review is the only production workflow pack available in this supervised pilot.</p><form className="inline-form" onSubmit={createProject}><input className="input" placeholder="Project name" required disabled={!writableOrganizations.length || busy} value={projectName} onChange={(event) => setProjectName(event.target.value)} /><button className="btn btn-primary" disabled={!organizationId || !writableOrganizations.length || busy} type="submit">Create project</button></form></article></section><section className="card"><div className="card-header"><div><h2>Your projects</h2><p className="muted">Only projects belonging to your organizations appear here.</p></div></div>{projects.length ? <div className="table-wrap"><table className="table"><thead><tr><th>Name</th><th>Workflow</th><th>Status</th><th>Updated</th></tr></thead><tbody>{projects.map((project) => <tr key={project.id}><td><a href={`/projects/${project.id}/evidence`}>{project.name}</a></td><td>{project.workflowType}</td><td><span className="status status-info">{project.status}</span></td><td>{new Date(project.updatedAt).toLocaleString()}</td></tr>)}</tbody></table></div> : <p className="empty">No production projects yet.</p>}</section></main>;
}
